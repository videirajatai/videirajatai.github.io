<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <title>Videira Jataí | Manifesto</title>
  
  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Syne:wght@700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  
  <style>
    /* RESET + GLOBAL */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: #0e0c1a;
      font-family: 'Space Grotesk', sans-serif;
      color: #f0eef7;
      overflow-x: hidden;
    }

    /* Cores radicais */
    :root {
      --bg-dark: #0a0814;
      --primary: #9b5de5;
      --primary-glow: #c77dff;
      --secondary: #fca311;
      --accent: #00f5d4;
      --card-bg: rgba(20, 18, 35, 0.85);
      --glass-border: rgba(255, 255, 255, 0.1);
      --shadow-neon: 0 0 15px rgba(155, 93, 229, 0.4);
    }

    /* Canvas de fundo animado */
    canvas#bg-canvas {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -2;
      opacity: 0.6;
    }

    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at 20% 30%, rgba(155,93,229,0.15), rgba(10,8,20,0.95));
      z-index: -1;
    }

    /* Scrollbar custom */
    ::-webkit-scrollbar {
      width: 6px;
    }
    ::-webkit-scrollbar-track {
      background: #1e1b2c;
    }
    ::-webkit-scrollbar-thumb {
      background: var(--secondary);
      border-radius: 10px;
    }

    /* HEADER FLUTUANTE */
    .header {
      position: fixed;
      top: 20px;
      left: 5%;
      right: 5%;
      width: 90%;
      padding: 0.8rem 2rem;
      background: rgba(10, 8, 20, 0.7);
      backdrop-filter: blur(16px);
      border-radius: 60px;
      z-index: 1000;
      border: 1px solid rgba(255,255,255,0.1);
      transition: all 0.3s;
    }
    .header.scrolled {
      top: 10px;
      background: rgba(10, 8, 20, 0.95);
      box-shadow: 0 8px 20px rgba(0,0,0,0.3);
    }
    .nav-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .logo img {
      height: 45px;
      filter: drop-shadow(0 0 4px var(--primary));
    }
    .logo h1 {
      font-family: 'Syne', sans-serif;
      font-size: 1.7rem;
      font-weight: 800;
      background: linear-gradient(135deg, #fff, var(--primary), var(--secondary));
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .nav-menu {
      display: flex;
      gap: 2rem;
      list-style: none;
    }
    .nav-menu a {
      text-decoration: none;
      font-weight: 500;
      color: #ddd;
      transition: 0.2s;
      font-size: 0.9rem;
    }
    .nav-menu a:hover {
      color: var(--secondary);
    }
    .btn-restrito {
      background: linear-gradient(95deg, var(--primary), var(--secondary));
      padding: 0.5rem 1.3rem;
      border-radius: 40px;
      color: #0e0c1a !important;
      font-weight: bold;
      box-shadow: var(--shadow-neon);
    }
    .mobile-menu-icon {
      display: none;
      font-size: 1.7rem;
      cursor: pointer;
      color: white;
    }

    /* HERO - seção principal em grade assimétrica */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: 6rem 5% 2rem;
      position: relative;
    }
    .hero-grid {
      display: grid;
      grid-template-columns: 1.2fr 0.8fr;
      gap: 4rem;
      align-items: center;
      width: 100%;
      max-width: 1400px;
      margin: 0 auto;
    }
    .hero-title {
      font-family: 'Syne', sans-serif;
      font-size: 4.5rem;
      font-weight: 800;
      line-height: 1.1;
    }
    .hero-title span {
      background: linear-gradient(125deg, var(--primary), var(--secondary));
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    .hero-description {
      font-size: 1.2rem;
      margin: 1.5rem 0;
      color: #ccc;
    }
    .hero-buttons {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }
    .btn-primary-neon {
      background: linear-gradient(95deg, var(--primary), #b967ff);
      border: none;
      padding: 0.9rem 2rem;
      border-radius: 60px;
      font-weight: bold;
      color: white;
      text-decoration: none;
      transition: 0.2s;
    }
    .btn-outline-neon {
      border: 2px solid var(--primary);
      padding: 0.9rem 2rem;
      border-radius: 60px;
      font-weight: bold;
      color: white;
      text-decoration: none;
    }
    .hero-stats {
      background: var(--card-bg);
      backdrop-filter: blur(12px);
      border-radius: 48px;
      padding: 2rem;
      border: 1px solid var(--glass-border);
    }
    .stats-number {
      font-size: 2.8rem;
      font-weight: 800;
      color: var(--secondary);
    }

    /* Seções com bordas arredondadas e gradientes */
    section {
      padding: 5rem 5%;
    }
    .container {
      max-width: 1300px;
      margin: 0 auto;
    }
    .section-title {
      font-family: 'Syne', sans-serif;
      font-size: 2.8rem;
      font-weight: 800;
      margin-bottom: 1rem;
      position: relative;
      display: inline-block;
    }
    .section-title:after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 0;
      width: 60%;
      height: 4px;
      background: linear-gradient(90deg, var(--primary), var(--secondary));
      border-radius: 4px;
    }
    .section-sub {
      color: #b9b3d4;
      margin-bottom: 3rem;
      font-size: 1.1rem;
    }

    /* Cards de missão - estilo flutuante */
    .cards-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 2rem;
      margin: 3rem 0;
    }
    .info-card {
      background: rgba(18, 16, 30, 0.7);
      backdrop-filter: blur(12px);
      border-radius: 32px;
      padding: 2rem;
      transition: all 0.3s;
      border: 1px solid rgba(255,255,255,0.05);
      text-align: center;
    }
    .info-card:hover {
      transform: translateY(-8px) scale(1.02);
      border-color: var(--primary);
      box-shadow: 0 0 30px rgba(155,93,229,0.3);
    }
    .info-card i {
      font-size: 2.8rem;
      color: var(--secondary);
      margin-bottom: 1rem;
    }

    /* Cards de cultos - laterais expansivas */
    .cultos-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 2rem;
      justify-content: center;
    }
    .culto-card {
      background: rgba(0,0,0,0.4);
      backdrop-filter: blur(10px);
      border-radius: 40px;
      padding: 1.8rem;
      flex: 1 1 280px;
      border: 1px solid rgba(255,255,255,0.1);
      transition: 0.2s;
    }
    .culto-card:hover {
      transform: translateY(-8px);
      border-color: var(--secondary);
    }
    .culto-titulo {
      font-size: 1.7rem;
      font-weight: 700;
      color: var(--accent);
    }

    /* Seção Células - cards em estilo "masonry" e botão flutuante */
    .cells-wrapper {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }
    .cell-card {
      background: rgba(25, 22, 45, 0.8);
      backdrop-filter: blur(8px);
      border-radius: 36px;
      padding: 1.5rem;
      transition: 0.25s;
      border: 1px solid rgba(255,255,255,0.08);
      position: relative;
    }
    .cell-card:hover {
      transform: translateY(-6px);
      border-color: var(--primary);
    }
    .cell-wp-button {
      position: absolute;
      top: 1rem;
      right: 1rem;
      background: #25D366;
      width: 38px;
      height: 38px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: 0.2s;
    }
    .cell-day {
      background: var(--primary);
      display: inline-block;
      padding: 0.2rem 1rem;
      border-radius: 40px;
      font-size: 0.8rem;
      font-weight: bold;
    }
    .cell-sector {
      font-size: 1.3rem;
      font-weight: 700;
      margin: 0.7rem 0;
      color: var(--secondary);
    }

    /* Ofertas - estilo glassmorphism */
    .ofertas-panel {
      display: flex;
      flex-wrap: wrap;
      gap: 2rem;
      background: rgba(0,0,0,0.3);
      backdrop-filter: blur(16px);
      border-radius: 64px;
      padding: 2rem;
      border: 1px solid rgba(255,255,255,0.1);
    }
    .qr-area {
      flex: 1;
      text-align: center;
    }
    .qr-area img {
      max-width: 180px;
      border-radius: 28px;
    }
    .pix-card {
      flex: 1;
    }
    .pix-key {
      background: #0e0c1a;
      padding: 0.6rem 1.2rem;
      border-radius: 60px;
      font-family: monospace;
      display: inline-block;
    }
    .btn-copy {
      background: var(--secondary);
      border: none;
      padding: 0.4rem 1rem;
      border-radius: 40px;
      cursor: pointer;
    }

    /* Encontro com Deus - destaque absoluto */
    .encontro-super {
      background: linear-gradient(125deg, #2b0f44, #0e0c1a);
      border-radius: 48px;
      padding: 3rem;
      text-align: center;
      margin: 2rem 0;
      border: 1px solid var(--primary);
    }

    /* Contato - estilo futurista */
    .contact-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
    }
    .contact-info, .contact-form {
      background: rgba(20, 18, 35, 0.6);
      backdrop-filter: blur(12px);
      border-radius: 32px;
      padding: 2rem;
    }
    footer {
      background: #05030f;
      text-align: center;
      padding: 2rem;
    }
    @media (max-width: 800px) {
      .hero-grid {
        grid-template-columns: 1fr;
        gap: 2rem;
      }
      .nav-menu {
        position: fixed;
        left: -100%;
        top: 80px;
        flex-direction: column;
        background: #0a0814;
        width: 70%;
        padding: 2rem;
        transition: 0.3s;
        border-radius: 20px;
      }
      .nav-menu.active {
        left: 5%;
      }
      .mobile-menu-icon {
        display: block;
      }
      .contact-grid {
        grid-template-columns: 1fr;
      }
      .section-title {
        font-size: 2rem;
      }
    }
    .reveal {
      opacity: 0;
      transform: translateY(30px);
      transition: 0.7s;
    }
    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }
  </style>
</head>
<body>

<canvas id="bg-canvas"></canvas>

<header class="header" id="header">
  <div class="nav-container">
    <div class="logo">
      <img src="logo.png" alt="Logo" onerror="this.src='https://placehold.co/60x60?text=V'">
      <h1>VIDEIRA//JATAÍ</h1>
    </div>
    <div class="mobile-menu-icon" id="mobileMenuIcon">
      <i class="fas fa-bars"></i>
    </div>
    <ul class="nav-menu" id="navMenu">
      <li><a href="#home">Início</a></li>
      <li><a href="#sobre">Visão</a></li>
      <li><a href="#cultos">Cultos</a></li>
      <li><a href="#celulas">Células</a></li>
      <li><a href="#ofertas">Ofertas PIX</a></li>
      <li><a href="#contato">Contato</a></li>
      <li><a href="https://videirajatai.com.br/sistema" target="_blank" class="btn-restrito">Área Restrita</a></li>
    </ul>
  </div>
</header>

<main>
  <!-- Hero diferentona -->
  <section id="home" class="hero">
    <div class="hero-grid">
      <div>
        <div class="hero-title">A fé <span>floresce</span><br>em comunidades</div>
        <div class="hero-description">Células que conectam, cultos que transformam. Uma nova geração da Videira em Jataí.</div>
        <div class="hero-buttons">
          <a href="#cultos" class="btn-primary-neon"><i class="fas fa-calendar-week"></i> Ver cultos</a>
          <a href="#celulas" class="btn-outline-neon">Células perto de você</a>
        </div>
      </div>
      <div class="hero-stats">
        <div><span class="stats-number">+15</span> <br>Células ativas</div>
        <div style="margin-top: 1rem;"><span class="stats-number">2</span> <br>Cultos semanais</div>
        <div style="margin-top: 1rem;"><i class="fas fa-map-marker-alt"></i> Av. Veriano de Oliveira Lima, 1067</div>
      </div>
    </div>
  </section>

  <!-- Sobre / Visão -->
  <section id="sobre">
    <div class="container">
      <h2 class="section-title reveal">Manifesto Videira</h2>
      <div class="section-sub reveal">Igreja de vencedores, cada membro um ministro</div>
      <div class="cards-grid">
        <div class="info-card reveal"><i class="fas fa-sun"></i><h3>Missão</h3><p>Ganhar, consolidar, discipular, treinar e enviar.</p></div>
        <div class="info-card reveal"><i class="fas fa-chart-line"></i><h3>Visão</h3><p>Edificar uma igreja onde cada lar é uma extensão do Reino.</p></div>
        <div class="info-card reveal"><i class="fas fa-gem"></i><h3>Valores</h3><p>Unidade, adoração genuína, compaixão e transparência.</p></div>
      </div>
      <div class="pastor-card" style="background: rgba(20,18,35,0.6); border-radius: 48px; padding: 2rem; backdrop-filter: blur(12px); margin-top: 2rem;">
        <div style="display: flex; flex-wrap: wrap; gap: 2rem; align-items: center;">
          <div style="flex:1; text-align: center;"><i class="fas fa-church" style="font-size: 4rem; color: var(--secondary);"></i></div>
          <div style="flex:3;">
            <h3 style="color: var(--accent);">NOSSA VISÃO</h3>
            <p>Nosso encargo é edificar uma igreja de vencedores, onde cada membro é um ministro, cada casa uma extensão da igreja. Alcançando assim, nossa geração pra Cristo através das células que se multiplicam.</p>
            <div style="background: rgba(0,0,0,0.4); padding: 1rem; border-radius: 28px; margin-top: 1rem;">
              <i class="fas fa-quote-left"></i> Atos 2:46,47 — "Todos os dias acrescentava o Senhor à igreja aqueles que se haviam de salvar."
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Cultos em destaque -->
  <section id="cultos" style="background: radial-gradient(circle at 80% 20%, #140e24, #0e0c1a);">
    <div class="container">
      <h2 class="section-title reveal">Celebrações</h2>
      <div class="section-sub reveal">Venha como você é</div>
      <div class="cultos-grid">
        <div class="culto-card">
          <div class="culto-titulo">Culto de Celebração</div>
          <div><i class="fas fa-clock"></i> Domingo 18h</div>
          <div><i class="fas fa-location-dot"></i> Av. Veriano de Oliveira Lima, 1067</div>
        </div>
        <div class="culto-card">
          <div class="culto-titulo">Culto KIDS</div>
          <div><i class="fas fa-child"></i> Domingo 18h</div>
          <div><i class="fas fa-map-pin"></i> Mesmo endereço - espaço kids</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Células Dinâmicas -->
  <section id="celulas">
    <div class="container">
      <h2 class="section-title reveal">Células de Crescimento</h2>
      <div class="section-sub reveal">Encontre sua célula perto de você</div>
      <div id="cellsContainer" class="cells-wrapper">
        <div style="width:100%; text-align:center;"><i class="fas fa-spinner fa-pulse"></i> Carregando células...</div>
      </div>
    </div>
  </section>

  <!-- Ofertas PIX -->
  <section id="ofertas">
    <div class="container">
      <h2 class="section-title reveal">Contribua com alegria</h2>
      <div class="section-sub reveal">Sua oferta transforma vidas</div>
      <div class="ofertas-panel">
        <div class="qr-area">
          <h3><i class="fas fa-qrcode"></i> PIX QR Code</h3>
          <img src="pix.png" alt="QR Code" onerror="this.src='https://api.qrserver.com/v1/create-qr-code/?size=180x180&data=00020126360014br.gov.bcb.pix011727.105.498/0001-03'">
        </div>
        <div class="pix-card">
          <h3>CNPJ para PIX</h3>
          <p><strong class="pix-key">27.105.498/0001-03</strong></p>
          <button class="btn-copy" onclick="copiarChave()">Copiar chave</button>
          <p style="margin-top: 1rem;">Qualquer valor – Deus ama quem contribui com alegria.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Encontro com Deus -->
  <div class="container">
    <div class="encontro-super">
      <h2><i class="fas fa-hands-praying"></i> Encontro com Deus</h2>
      <p>Viva uma experiência única de renovação espiritual. Inscreva-se!</p>
      <a href="https://www.videirajatai.com.br/encontrocomdeus" target="_blank" class="btn-primary-neon" style="display: inline-block; margin-top: 1rem;">QUERO PARTICIPAR →</a>
    </div>
  </div>

  <!-- Contato -->
  <section id="contato">
    <div class="container">
      <h2 class="section-title reveal">Fale conosco</h2>
      <div class="section-sub reveal">Estamos prontos para orar e acolher você</div>
      <div class="contact-grid">
        <div class="contact-info">
          <p><i class="fas fa-map-marker-alt"></i> Av. Veriano de Oliveira Lima, 1067, Santa Maria, Jataí - GO</p>
          <p><i class="fab fa-whatsapp"></i> (62) 99623-1730</p>
          <p><i class="fas fa-envelope"></i> videirajatai01@gmail.com</p>
          <div style="margin: 1rem 0;">
            <a href="https://instagram.com/videirajatai" target="_blank" style="color: var(--secondary); font-size: 1.8rem; margin-right: 1rem;"><i class="fab fa-instagram"></i></a>
            <a href="https://www.youtube.com/@videirajatai" target="_blank" style="color: var(--secondary); font-size: 1.8rem;"><i class="fab fa-youtube"></i></a>
          </div>
          <a href="https://www.google.com/maps/search/?api=1&query=Av.+Veriano+de+Oliveira+Lima,+1067,+Jataí" target="_blank" class="btn-outline-neon">Abrir mapa</a>
        </div>
        <div class="contact-form">
          <form id="contactForm">
            <input type="text" placeholder="Seu nome" id="nome" style="width:100%; padding:12px; margin:10px 0; background:#1e1b2c; border:1px solid #3a2f4f; border-radius: 30px; color:white;">
            <input type="email" placeholder="Seu e-mail" id="email" style="width:100%; padding:12px; margin:10px 0; background:#1e1b2c; border:1px solid #3a2f4f; border-radius: 30px; color:white;">
            <textarea rows="4" placeholder="Mensagem ou pedido de oração" id="msg" style="width:100%; padding:12px; border-radius: 28px; background:#1e1b2c; border:1px solid #3a2f4f; color:white;"></textarea>
            <button type="submit" class="btn-primary-neon" style="border:none; cursor:pointer;">Enviar mensagem</button>
          </form>
        </div>
      </div>
    </div>
  </section>
</main>

<footer>
  <div class="footer-links">
    <a href="#home" style="color:#ccc; margin:0 1rem;">Início</a>
    <a href="#sobre" style="color:#ccc; margin:0 1rem;">Visão</a>
    <a href="#cultos" style="color:#ccc; margin:0 1rem;">Cultos</a>
    <a href="#celulas" style="color:#ccc; margin:0 1rem;">Células</a>
    <a href="#ofertas" style="color:#ccc; margin:0 1rem;">Ofertas</a>
    <a href="https://videirajatai.com.br/sistema">Área do Membro</a>
  </div>
  <p>© 2026 Igreja Videira de Jataí | Pr. Germerson Araujo</p>
  <p>@videirajatai | Av. Veriano de Oliveira Lima, 1067</p>
</footer>

<script>
  // Partículas / efeito canvas fundo
  const canvas = document.getElementById('bg-canvas');
  const ctx = canvas.getContext('2d');
  let width = window.innerWidth;
  let height = window.innerHeight;
  let particles = [];

  function resizeCanvas() {
    width = window.innerWidth;
    height = window.innerHeight;
    canvas.width = width;
    canvas.height = height;
  }
  window.addEventListener('resize', resizeCanvas);
  resizeCanvas();

  function initParticles() {
    particles = [];
    for (let i = 0; i < 100; i++) {
      particles.push({
        x: Math.random() * width,
        y: Math.random() * height,
        radius: Math.random() * 2 + 1,
        alpha: Math.random() * 0.5 + 0.2,
        vx: (Math.random() - 0.5) * 0.3,
        vy: (Math.random() - 0.5) * 0.2
      });
    }
  }
  function animateParticles() {
    ctx.clearRect(0, 0, width, height);
    for (let p of particles) {
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(155, 93, 229, ${p.alpha})`;
      ctx.fill();
      p.x += p.vx;
      p.y += p.vy;
      if (p.x < 0) p.x = width;
      if (p.x > width) p.x = 0;
      if (p.y < 0) p.y = height;
      if (p.y > height) p.y = 0;
    }
    requestAnimationFrame(animateParticles);
  }
  initParticles();
  animateParticles();

  // --- Mobile menu ---
  const mobileIcon = document.getElementById('mobileMenuIcon');
  const navMenu = document.getElementById('navMenu');
  mobileIcon.addEventListener('click', () => {
    navMenu.classList.toggle('active');
    const icon = mobileIcon.querySelector('i');
    if (navMenu.classList.contains('active')) icon.classList.replace('fa-bars', 'fa-times');
    else icon.classList.replace('fa-times', 'fa-bars');
  });
  document.querySelectorAll('.nav-menu a').forEach(link => {
    link.addEventListener('click', () => {
      if (window.innerWidth <= 800) navMenu.classList.remove('active');
    });
  });

  // Header scroll
  const header = document.getElementById('header');
  window.addEventListener('scroll', () => {
    if (window.scrollY > 40) header.classList.add('scrolled');
    else header.classList.remove('scrolled');
  });

  // Reveal on scroll
  const reveals = document.querySelectorAll('.reveal');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
  }, { threshold: 0.1 });
  reveals.forEach(el => observer.observe(el));

  // --- API de Células (mesma URL) ---
  const API_URL = "https://script.google.com/macros/s/AKfycbxnystn3UqAKnfLUgw7tXS8CzrnpxTS0iax80rhMCRs-T47ibodGYvbRw2KRrtsnCzn/exec";
  const PASTOR_WPP = "5562996231730";

  async function carregarCelulas() {
    const container = document.getElementById('cellsContainer');
    if (!container) return;
    container.innerHTML = '<div style="width:100%; text-align:center;"><i class="fas fa-spinner fa-pulse"></i> Carregando células...</div>';
    try {
      let res = await fetch(API_URL, { cache: "no-store" });
      let dados = await res.json();
      if (dados?.dados && Array.isArray(dados.dados)) dados = dados.dados;
      if (!Array.isArray(dados)) dados = [];
      if (dados.length === 0) {
        container.innerHTML = '<div class="cell-card" style="width:100%;">Nenhuma célula disponível ainda</div>';
        return;
      }
      container.innerHTML = '';
      dados.forEach(cell => {
        const diaRaw = cell.dia || cell.dia_horaio || cell.dia_horario || '';
        let dia = "Dia a definir";
        const dias = ["Domingo","Segunda","Terça","Quarta","Quinta","Sexta","Sábado"];
        for (let d of dias) if (diaRaw.includes(d)) { dia = d; break; }
        if (dia === "Dia a definir" && diaRaw) dia = diaRaw.split(" ")[0];
        let setor = cell.bairro || "";
        if (!setor || setor === "Setor") {
          if (cell.endereco && cell.endereco.includes(" - ")) setor = cell.endereco.split(" - ").pop().trim();
          else if (cell.endereco) setor = cell.endereco.split(",")[0].trim();
        }
        let lider = cell.lider || "Irmão(ã)";
        if (lider.includes("@")) lider = "Irmão(ã)";
        const endereco = cell.endereco || "Endereço não informado";
        const card = document.createElement('div');
        card.className = 'cell-card';
        card.innerHTML = `
          <div class="cell-day">${dia}</div>
          <div class="cell-sector">📍 ${setor}</div>
          <div><i class="fas fa-user"></i> ${lider}</div>
          <div style="font-size:0.75rem; margin-top: 0.5rem;"><i class="fas fa-house"></i> ${endereco}</div>
          <a href="https://wa.me/${PASTOR_WPP}?text=Olá%20Pastor,%20gostaria%20de%20saber%20mais%20sobre%20a%20célula%20de%20${encodeURIComponent(dia)}%20no%20${encodeURIComponent(setor)}" target="_blank" class="cell-wp-button"><i class="fab fa-whatsapp"></i></a>
        `;
        container.appendChild(card);
      });
    } catch (err) {
      container.innerHTML = '<div class="cell-card" style="width:100%;">Erro ao carregar células. Tente mais tarde.</div>';
    }
  }

  // Form contato
  document.getElementById('contactForm')?.addEventListener('submit', (e) => {
    e.preventDefault();
    alert('Mensagem enviada! Em breve retornaremos.');
    e.target.reset();
  });

  window.copiarChave = () => {
    navigator.clipboard.writeText('27.105.498/0001-03').then(() => alert('Chave PIX copiada!'));
  };

  document.addEventListener('DOMContentLoaded', () => {
    carregarCelulas();
    reveals.forEach(el => { if (el.getBoundingClientRect().top < window.innerHeight) el.classList.add('visible'); });
  });
</script>
</body>
</html>
